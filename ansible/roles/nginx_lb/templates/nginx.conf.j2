events {}
 
http {
  upstream backend {
    {% for h in groups['app'] %}
    server {{ h }}:3000;
    {% endfor %}
  }
 
  server {
    listen 80;
    location / {
      proxy_pass http://backend;
    }
  }
}

 